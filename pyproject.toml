[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "patrol-lens"
version = "0.2.0"
description = "Retrieval-guided Gemini active perception for body-worn camera search"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
openrouter = ["openai>=1.0"]
# Compatibility alias: the reasoning model remains Gemini, while transport is OpenRouter.
gemini = ["openai>=1.0"]
postgres = ["psycopg[binary]>=3.2", "pgvector>=0.3"]
vision = [
  "numpy>=1.26",
  "pillow>=10",
  "torch>=2.2",
  "transformers>=4.45",
  "faiss-cpu>=1.8",
]
speech = [
  "faster-whisper>=1.0",
]
ocr = ["paddleocr>=3", "paddlepaddle>=3"]
audio = ["silero-vad>=5", "tensorflow>=2.16", "tensorflow-hub>=0.16"]
full = [
  "openai>=1.0",
  "psycopg[binary]>=3.2",
  "pgvector>=0.3",
  "numpy>=1.26",
  "pillow>=10",
  "torch>=2.2",
  "transformers>=4.45",
  "faiss-cpu>=1.8",
  "faster-whisper>=1.0",
  "paddleocr>=3",
  "paddlepaddle>=3",
  "silero-vad>=5",
  "tensorflow>=2.16",
  "tensorflow-hub>=0.16",
]
dev = ["pytest>=8"]

[project.scripts]
patrol-lens = "patrol_lens.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
